import type { Metadata, Viewport } from 'next';

export const metadata: Metadata = {
  title: 'Logout | SKRBL AI',
  description: 'You\'ve been securely logged out of SKRBL AI.',
};

export const viewport: Viewport = {
  width: 'device-width',
  initialScale: 1,
  maximumScale: 1,
  userScalable: false,
};