import { ReactNode } from 'react';

export const metadata = {
  title: "SKRBL AI Social Media – Social Media Domination Platform",
  description: "Dominate social media with AI-powered viral content generation and automated posting.",
};

export const dynamic = 'force-dynamic';

export default function SocialMediaLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
} 