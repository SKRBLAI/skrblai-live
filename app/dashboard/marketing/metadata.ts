import type { Metadata, Viewport } from 'next';

export const metadata: Metadata = {
  title: 'Marketing Dashboard | SKRBL AI',
  description: 'Automate and analyze your marketing efforts with SKRBL AI.',
};

export const viewport: Viewport = {
  width: 'device-width',
  initialScale: 1,
  maximumScale: 1,
  userScalable: false,
};