import type { Metadata, Viewport } from 'next';

export const metadata: Metadata = {
  title: 'Social Media Dashboard | SKRBL AI',
  description: 'Plan, automate, and track your social media content strategy.',
};

export const viewport: Viewport = {
  width: 'device-width',
  initialScale: 1,
  maximumScale: 1,
  userScalable: false,
};